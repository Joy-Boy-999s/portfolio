import { useToast } from './Toast'

const ToastNotification = () => {
  const { showToast } = useToast()

  // This component is just for demonstration
  // The actual toast display is handled in ToastProvider
  return null
}

export default ToastNotification